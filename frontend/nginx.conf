# frontend/nginx.conf 파일 내용

server {
    listen 80;

    # 웹사이트 파일이 위치할 기본 경로
    root /usr/share/nginx/html;
    index index.html;

    # 모든 요청에 대해 SPA 라우팅 처리
    location / {
        # 요청된 파일이나 폴더가 없으면 index.html을 반환
        try_files $uri $uri/ /index.html;
    }
}